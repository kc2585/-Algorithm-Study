#include<iostream>
#include<algorithm>
#include<string>
using namespace std;

int arr[9];

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);

	cin >> n;
	for (int i = 0; i < 6; i++) {
		cin >> dice[i];
	}
	sol();
}